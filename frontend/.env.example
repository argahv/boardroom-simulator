# ──────────────────────────────────────────────
# Boardroom Simulator — Frontend Environment
# ──────────────────────────────────────────────
# Copy to .env.local to activate:
#   cp .env.example .env.local

# Backend API URL (required)
# For local dev with make backend:
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
